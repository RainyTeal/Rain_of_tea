# 复制本文件为 .env 并填入你自己的值，不要把真实密钥提交到仓库

# DeepSeek API（必填）
DEEPSEEK_APIKEY=

# OpenAI API（可选）
OPENAI_APIKEY=

# 会话签名密钥（必填，使用足够长的随机字符串，如：openssl rand -hex 32）
SECRET_KEY=

# 文件上传根目录（可选，默认 ./ftp）
FTP_PATH=ftp

# 内网穿透（可选，默认不启用）
FRP_TOKEN=
FRP_PORT=
